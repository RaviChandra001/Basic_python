# Ternary Operator in python 
is_friend = True 
can_msg = "message allowed" if is_friend else "not allowed"

print(can_msg)

is_friend = False
can_msg2 = "Message allowed" if is_friend else "not Allowed"

print(can_msg2)